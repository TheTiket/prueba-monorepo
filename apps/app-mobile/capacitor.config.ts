import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-mobile',
  webDir: '../../dist/apps/app-mobile',
  bundledWebRuntime: false,
};

export default config;
